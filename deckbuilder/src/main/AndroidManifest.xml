<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wanderingbme.warhammerunderworldsdeckbuilder">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_underworlds"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_underworlds_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseWarbandActivity">

            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value=".HomeScreen" /> -->
        </activity>
        <activity android:name=".BuildWarband">

            <!-- <meta-data -->
            <!-- android:name="android.support.PARENT_ACTIVITY" -->
            <!-- android:value=".ChooseWarbandActivity" /> -->
        </activity>
        <activity android:name=".AddCardActivity" />
        <activity android:name=".LibraryActivity"></activity>
    </application>

</manifest>